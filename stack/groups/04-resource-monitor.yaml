template:
  path: resource_monitor.yaml

entries:
  - name: DBT_PROVISION_APP_WH_MONITOR
    frequency: MONTHLY
    start_time: IMMEDIATELY
    comment: Resource monitor for dbt provisioning warehouse
    triggers:
      - threshold: 75
        action: NOTIFY
      - threshold: 100
        action: SUSPEND