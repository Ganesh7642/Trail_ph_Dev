template:
  path: resource_monitor.yaml

entries:
  - name: PROVISION_APP_WH_MONITOR
    credit_quota: 50
    frequency: WEEKLY
    start_time: IMMEDIATELY
    comment: Resource monitor for Provision App warehouse
    triggers:
      - threshold: 75
        action: NOTIFY
      - threshold: 100
        action: SUSPEND