template:
  path: warehouse.yml

entries:
  - wh_name: DBT_PROVISION_APP_WH
    wh_size: XSMALL
    wh_type: STANDARD
    resource_type: STANDARD_GEN_2
    auto_resume: true
    initially_suspended: true
    statement_queued_timeout_in_seconds: 0
    statement_timeout_in_seconds: 0
    resource_monitor: DBT_PROVISION_APP_WH_MONITOR
    comment: Warehouse for dbt provisioning workload