resourceMonitors:
  - name: {{ name }}
    properties:
      credit_quota: {{ credit_quota }}
      frequency: {{ frequency }}
      start_timestamp: {{ start_time }}
      comment: "{{ comment }}"
      triggers:
{% for t in triggers | default([]) %}
        - threshold: {{ t.threshold }}
          action: {{ t.action }}
{% endfor %}