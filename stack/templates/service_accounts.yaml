users:
  - name: {{ user_name }}
    properties:
      comment: "{{ comment }}"
      disabled: {{ disabled | default(false) }}
      must_change_password: {{ must_change_password | default(false) }}
      default_warehouse: {{ default_warehouse }}
      default_role: {{ default_role }}
      default_secondary_roles: 'ALL'
